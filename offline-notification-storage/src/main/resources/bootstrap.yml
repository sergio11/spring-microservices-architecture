spring:
  application:
    name: offline-notification-storage
  cloud:
    config:
      uri: http://localhost:8888

---
spring:
  profiles: docker
  cloud:
    config:
      failFast: true
      uri: http://config-service:8888  
