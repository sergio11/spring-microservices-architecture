spring:
  application:
    name: account-worker-service
  cloud:
    config:
      uri: http://localhost:8888

---
spring:
  profiles: docker
  cloud:
    config:
      failFast: true
      uri: http://config-service:8888  
