server:
    port: ${PORT:8761}
eureka:
    dashboard:
        path: /eureka-dashboard
    client:
        registerWithEureka: false
        fetchRegistry: false
    server:
        waitTimeInMsWhenSyncEmpty: 0 
      